# Required in Vercel so the deployed frontend calls Render instead of localhost.
VITE_API_BASE_URL=https://your-render-service.onrender.com

# Used for generated links and callbacks shown inside the app.
VITE_FRONTEND_DOMAIN=https://your-vercel-app.vercel.app
